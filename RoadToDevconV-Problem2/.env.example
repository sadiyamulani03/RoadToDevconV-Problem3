# Copy to .env and fill in real values. Never commit .env.
# No secrets are stored in this repository. Placeholders only.

# Optional local Bee endpoint for development (unused by subsidised gateway path).
BEE_API_URL=http://localhost:1633

# Optional display name for the observer. Not a secret.
OBSERVER_NAME=
